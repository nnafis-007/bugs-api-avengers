# JWT Authentication Flow - Visual Guide

## 🔄 Complete User Journey

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER VISITS "/" (ROOT PAGE)                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
            ┌────────────────────────────────┐
            │  Check localStorage for Token  │
            └────────────┬───────────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
    NO TOKEN                      TOKEN EXISTS
          │                             │
          ▼                             ▼
┌──────────────────┐       ┌──────────────────────────┐
│  SHOW LOGIN PAGE │       │ Verify Token with Backend │
│                  │       │  GET /api/profile         │
│  [Login Form]    │       └──────────┬────────────────┘
│  [Register Form] │                  │
└──────────────────┘        ┌─────────┴──────────┐
                            │                    │
                       TOKEN VALID        TOKEN EXPIRED/INVALID
                            │                    │
                            ▼                    ▼
                 ┌────────────────────┐  ┌─────────────────────┐
                 │  SHOW PROFILE PAGE │  │ Try Refresh Token   │
                 │                    │  │ POST /api/refresh   │
                 │  ✓ Logged in as:   │  └──────┬──────────────┘
                 │    user@email.com  │         │
                 │  [Logout]          │  ┌──────┴────────┐
                 │                    │  │               │
                 │  [Your Profile]    │  REFRESH      REFRESH
                 │  [View Details]    │  SUCCESS      FAILED
                 │  [Products]        │  │               │
                 └────────────────────┘  │               ▼
                            ▲            │     ┌──────────────────┐
                            │            │     │  LOGOUT & CLEAR  │
                            └────────────┘     │  Show Login Page │
                                               └──────────────────┘
```

---

## 🔑 Login Flow

```
USER NOT LOGGED IN
       │
       ▼
┌────────────────┐
│  Login Form    │
│  [email]       │
│  [password]    │
│  [Login] btn   │
└────────┬───────┘
         │ Click Login
         ▼
┌─────────────────────┐
│  POST /api/login    │
│  {email, password}  │
└─────────┬───────────┘
          │
    ┌─────┴──────┐
    │            │
VALID CREDS  INVALID CREDS
    │            │
    ▼            ▼
┌─────────────────────────┐  ┌──────────────────┐
│ Receive Tokens          │  │ Show Error       │
│ {                       │  │ "Invalid         │
│   accessToken,          │  │  credentials"    │
│   refreshToken,         │  └──────────────────┘
│   user: {email, id}     │
│ }                       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Store in localStorage:  │
│ - accessToken           │
│ - refreshToken          │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Update React State:     │
│ - setIsAuthenticated    │
│ - setUser               │
│ - setAccessToken        │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ REDIRECT TO PROFILE     │
│ Show: "Welcome back!"   │
│ Auto-load products      │
└─────────────────────────┘
```

---

## 🔄 Page Refresh Flow (THE FIX!)

```
USER ALREADY LOGGED IN
(has token in localStorage)
       │
       ▼
┌────────────────────┐
│ User refreshes     │
│ page (F5) or       │
│ visits "/" again   │
└────────┬───────────┘
         │
         ▼
┌────────────────────────┐
│ useEffect() runs       │
│ on component mount     │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────────┐
│ Read from localStorage:    │
│ const token =              │
│   localStorage             │
│   .getItem('accessToken')  │
└────────┬───────────────────┘
         │
    ┌────┴─────┐
    │          │
  FOUND      NOT FOUND
    │          │
    ▼          ▼
┌──────────────────┐  ┌──────────────┐
│ Show "Loading..."│  │ Show Login   │
└────────┬─────────┘  │ Form         │
         │            └──────────────┘
         ▼
┌──────────────────────────┐
│ Verify Token:            │
│ GET /api/profile         │
│ Authorization: Bearer... │
└────────┬─────────────────┘
         │
    ┌────┴────┐
    │         │
  200 OK   401 ERROR
    │         │
    ▼         ▼
┌────────────────┐  ┌──────────────────┐
│ ✅ VALID       │  │ Try Token Refresh│
│                │  │ (if have refresh)│
│ Show Profile   │  └────┬─────────────┘
│ Page           │       │
│                │  ┌────┴─────┐
│ "Welcome back, │  │          │
│  user@email!"  │  SUCCESS  FAIL
│                │  │          │
└────────────────┘  │          ▼
         ▲          │    ┌──────────┐
         │          │    │  Logout  │
         └──────────┘    │  & Show  │
                         │  Login   │
                         └──────────┘
```

---

## 🚪 Logout Flow

```
USER CLICKS LOGOUT BUTTON
       │
       ▼
┌─────────────────────────┐
│ handleLogout()          │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ POST /api/logout            │
│ Authorization: Bearer...    │
│ Body: {refreshToken}        │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Backend adds tokens to      │
│ blacklist (can't be used)   │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Clear localStorage:         │
│ removeItem('accessToken')   │
│ removeItem('refreshToken')  │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ Clear React State:          │
│ setAccessToken(null)        │
│ setRefreshToken(null)       │
│ setUser(null)               │
│ setIsAuthenticated(false)   │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ SHOW LOGIN PAGE             │
│ Message: "Logged out        │
│          successfully"      │
└─────────────────────────────┘
```

---

## ⚡ Automatic Token Refresh Flow

```
USER MAKES API REQUEST
(after 15 minutes - token expired)
       │
       ▼
┌─────────────────────────┐
│ GET /api/profile        │
│ Authorization: Bearer   │
│   <expired_token>       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Backend Response:       │
│ 401 Unauthorized        │
│ {                       │
│   error: "Token expired"│
│ }                       │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Frontend Detects        │
│ "Token expired" error   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ Automatic Action:       │
│ refreshAccessToken()    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ POST /api/refresh       │
│ Body: {refreshToken}    │
└────────┬────────────────┘
         │
    ┌────┴────┐
    │         │
REFRESH     REFRESH
VALID      EXPIRED
    │         │
    ▼         ▼
┌────────────────────┐  ┌──────────────┐
│ Get New Access     │  │ Force        │
│ Token              │  │ Re-login     │
└────────┬───────────┘  └──────────────┘
         │
         ▼
┌─────────────────────────┐
│ Update localStorage     │
│ with new accessToken    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│ RETRY Original Request  │
│ with new token          │
│                         │
│ ✅ Request succeeds!    │
│                         │
│ User didn't notice      │
│ anything happened       │
└─────────────────────────┘
```

---

## 🎯 State Management

```
┌──────────────────────────────────────────────────┐
│           REACT STATE VARIABLES                   │
├──────────────────────────────────────────────────┤
│                                                   │
│  accessToken        → Current access token       │
│  refreshToken       → Current refresh token      │
│  user               → {id, email, created_at}    │
│  isAuthenticated    → true/false                 │
│  loading            → true/false (initial check) │
│  message            → Status messages            │
│  products           → Array of products          │
│  showProducts       → true/false                 │
│                                                   │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│           LOCALSTORAGE KEYS                       │
├──────────────────────────────────────────────────┤
│                                                   │
│  accessToken        → JWT access token string    │
│  refreshToken       → JWT refresh token string   │
│                                                   │
│  ⚠️  Persists across page refreshes              │
│  ⚠️  Persists across browser restarts            │
│  ⚠️  Shared across tabs in same domain           │
│                                                   │
└──────────────────────────────────────────────────┘
```

---

## 📊 Timeline Example

```
TIME        EVENT                           STATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
00:00       User logs in                    ✅ isAuthenticated = true
            Token expires in: 15m           🔑 Access token valid
            
00:14       User still browsing             ✅ isAuthenticated = true
            Token expires in: 1m            🔑 Access token valid
            
00:15       Access token expires            ✅ isAuthenticated = true
            User clicks "View Profile"      ⏰ Access token expired
            
00:15:01    Frontend detects expired        🔄 Refreshing...
            Calls /api/refresh              
            
00:15:02    Gets new access token           ✅ isAuthenticated = true
            Profile loads successfully      🔑 New access token valid
            User didn't notice!             (expires in 15m)
            
7 days      Refresh token expires           ⏰ Refresh token expired
later       User must re-login              ❌ Force logout
```

---

## 🔒 Security Flow

```
TOKEN VERIFICATION STEPS
┌─────────────────────────┐
│ 1. Check Format         │ → Must be "Bearer <token>"
├─────────────────────────┤
│ 2. Check Blacklist      │ → Not in logout blacklist
├─────────────────────────┤
│ 3. Verify Signature     │ → HMAC-SHA256 valid
├─────────────────────────┤
│ 4. Check Expiration     │ → Not past exp timestamp
├─────────────────────────┤
│ 5. Verify Issuer        │ → iss = "bugs-api-avengers"
├─────────────────────────┤
│ 6. Verify Audience      │ → aud = "bugs-api-client"
└─────────────────────────┘
         │
    All Pass? ──YES──→ ✅ Allow Request
         │
        NO
         │
         ▼
    ❌ Reject with 401
```

---

## 🎉 THE FIX - Before vs After

### ❌ BEFORE (Problem)
```
1. User logs in successfully
2. Token stored only in React state
3. User refreshes page (F5)
4. React state resets to initial values
5. Token lost!
6. Shows login page again ❌
```

### ✅ AFTER (Fixed)
```
1. User logs in successfully
2. Token stored in:
   - React state (for current session)
   - localStorage (persists)
3. User refreshes page (F5)
4. useEffect checks localStorage
5. Token found!
6. Verifies token with backend
7. Shows profile page immediately ✅
```

---

## 🎨 UI Flow

```
LOADING STATE
┌──────────────────────┐
│ 🔄 Loading...        │
│ Checking auth...     │
└──────────────────────┘
        ↓ (1-2 seconds)
        ↓
   ┌────┴─────┐
   │          │
LOGGED IN  NOT LOGGED IN
   │          │
   ▼          ▼
┌──────────────────────┐  ┌──────────────────────┐
│ ✓ user@example.com   │  │ Please Login         │
│ [Logout]             │  │ [email]              │
│                      │  │ [password]           │
│ Your Profile         │  │ [Login] [Register]   │
│ [View Details]       │  └──────────────────────┘
│ [Refresh Products]   │
│                      │
│ Welcome back!        │
│                      │
│ [Product Cards...]   │
└──────────────────────┘
```

---

This visual guide shows exactly how JWT authentication works in your application now! 🚀
